#include <stdio.h>

/**
 * _strcpy - copies string taken as input to another
 * @src: the string being attached to dest
 * @dest: is the string that @src is being attached to
 * Return: string or combination of src and dest
 */
char *_strcpy(char *dest, char *src)
{
}
