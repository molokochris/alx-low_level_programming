#include <stdio.h>

/**
 * mul - takes in two integers and find the product of the two
 * @a: first integer
 * @b: second integer
 * Return: retruns the product
 */
int mul(int a, int b)
{
	return (a * b);
}
