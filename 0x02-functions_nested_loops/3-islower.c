#include "main.h"

/**
 * _islower - checks if character passed is lower case or not
 * @c: is the input passed into the function _islower
 * Return: returns 1 for true and 0 for false
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
		return (1);
return (0);
}
